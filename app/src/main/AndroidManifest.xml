<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.xinicompany.annonymate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnnonyMate"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".ResetPasswordActivity"></activity>
        <activity android:name=".text_view" />
        <activity android:name=".cardViewMessage" />
        <activity android:name=".personalMessage" />
        <activity android:name=".allMessages"></activity>
        <activity android:name=".MatchFound" />
        <activity android:name=".findingPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" />
        <activity android:name=".LoginPage" />
        <activity android:name=".WelcomePage"></activity>
    </application>

</manifest>